export enum Status {
  Enable = 0,
  Disable = 1,
}

export enum ApiSource {
  OpenAI = 'openai',
}

export enum UserRole {
  User = 0,
  Admin = 1,
}

export enum SSEStatus {
  Open,
  Message,
  Close,
  Error,
}
